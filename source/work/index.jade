extends ../_layouts/_default
block Vars
  -mainMenu = true
  -relativeDir = '../'
  -section = "work"
block MainContent
  div.Main.Main--layout-stageLeft.Work
    -if (typeof(workToken) !== "undefined")
      include _worklist
    -else
      include _sidebar